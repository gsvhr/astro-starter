---
const { article } = Astro.props;
---

<article class="bg-white rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow duration-300">
	<div class="p-6">
		<h2 class="text-xl font-semibold text-gray-800 mb-3">
			<a href={`/blog/${article.slug}`}>{article.title}</a>
		</h2>
		<div class="flex items-center text-sm text-gray-500 mb-3">
			<time datetime={article.date}>{new Date(article.date).toLocaleDateString('ru-RU', { year: 'numeric', month: 'long', day: 'numeric' })}</time>
			<span class="mx-2">•</span>
			<span>{article.category}</span>
		</div>
		<p class="text-gray-600 mb-4">{article.description}</p>
		<a href={`/blog/${article.slug}`} class="inline-block text-blue-600 hover:text-blue-800 font-medium">
			Читать далее →
		</a>
		</div>
</article>